{
  "name": "cyclegraph-root",
  "private": true,
  "version": "0.7.3",
  "description": "Root-level tooling for schema validation and CI checks",
  "scripts": {
    "test:schema:compile": "ajv compile -c ajv-formats --spec=draft7 -s schema/session_metrics.v1.json && ajv compile -c ajv-formats --spec=draft7 -s schema/profile.v1.json",
    "test:schema": "npm run test:schema:compile"
  },
  "devDependencies": {
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1",
    "vite": "^7.2.2"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "zod": "^4.1.12"
  }
}
